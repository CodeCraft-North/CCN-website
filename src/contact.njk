---
layout: base.njk
title: "Contact CodeCraft North"
description: "Get in touch with CodeCraft North. Start a new project, ask a question, or arrange a free discovery call. Based in the Wirral, serving clients across the UK."
date: 2026-02-01
---

<!-- Hero Section -->
<section class="py-24 text-center relative overflow-hidden bg-bg-primary dark:bg-bg-primary-dark">
    <div aria-hidden="true" class="pointer-events-none absolute inset-0">
        <div class="absolute -top-32 -left-32 w-96 h-96 rounded-full bg-brand-light dark:bg-brand-dark/40 blur-3xl opacity-70" style="animation: var(--animate-drift);"></div>
        <div class="absolute -bottom-40 -right-20 w-md h-112 rounded-full bg-brand-primary/10 dark:bg-brand-primary-on-dark/10 blur-3xl" style="animation: var(--animate-drift-slow); animation-delay: -6s;"></div>
    </div>
    <div class="max-w-6xl mx-auto px-6 relative z-10">
        <div class="max-w-4xl mx-auto hero-entry">
            <span class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-brand-light dark:bg-brand-dark/50 text-brand-primary dark:text-brand-primary-on-dark text-sm font-semibold tracking-wide mb-6 mt-6">
                <span class="w-2 h-2 rounded-full bg-brand-primary animate-pulse"></span>
                We'd love to hear from you
            </span>
            <h1 class="text-5xl md:text-6xl font-black mb-6 text-text-primary dark:text-text-strong-dark leading-tight">Let's start building <span class="text-brand-primary dark:text-brand-primary-on-dark">something great</span></h1>
            <p class="text-2xl font-semibold text-text-primary dark:text-text-strong-dark mb-6 leading-snug">No sales pitch, no pressure — just a clear conversation.</p>
            <div class="mb-8 flex justify-center">
                <div class="w-full max-w-md min-w-0">
                    {% image "./src/img/contact-us.svg", "Contact CodeCraft North for website services", [400, 800, 1200], "w-full h-auto max-w-full rounded-lg" %}
                </div>
            </div>
            <p class="text-lg mb-10 text-text-secondary dark:text-text-secondary-dark leading-relaxed max-w-3xl mx-auto text-left">
                Whether you're ready to begin a new website project or just exploring options, we'd love to hear from you. At CodeCraft North, every project starts with a conversation — no sales pitch, no pressure. We'll listen, understand what your business needs and advise on the best approach to help you grow online.
            </p>
            <ul class="grid grid-cols-2 md:grid-cols-4 gap-4 max-w-3xl mx-auto">
                <li class="flex items-center gap-3 justify-center text-left">
                    <span class="shrink-0 w-9 h-9 rounded-lg bg-brand-light dark:bg-brand-dark/50 flex items-center justify-center text-brand-primary dark:text-brand-primary-on-dark">
                        <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M21.75 6.75v10.5a2.25 2.25 0 01-2.25 2.25h-15a2.25 2.25 0 01-2.25-2.25V6.75m19.5 0A2.25 2.25 0 0019.5 4.5h-15a2.25 2.25 0 00-2.25 2.25m19.5 0v.243a2.25 2.25 0 01-1.07 1.916l-7.5 4.615a2.25 2.25 0 01-2.36 0L3.32 8.91a2.25 2.25 0 01-1.07-1.916V6.75"/></svg>
                    </span>
                    <span class="text-sm font-semibold text-text-primary dark:text-text-strong-dark leading-tight">We reply within 1 business day</span>
                </li>
                <li class="flex items-center gap-3 justify-center text-left">
                    <span class="shrink-0 w-9 h-9 rounded-lg bg-brand-light dark:bg-brand-dark/50 flex items-center justify-center text-brand-primary dark:text-brand-primary-on-dark">
                        <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75m-3-7.036A11.959 11.959 0 013.598 6 11.99 11.99 0 003 9.749c0 5.592 3.824 10.29 9 11.623 5.176-1.332 9-6.03 9-11.622 0-1.31-.21-2.571-.598-3.751h-.152c-3.196 0-6.1-1.248-8.25-3.285z"/></svg>
                    </span>
                    <span class="text-sm font-semibold text-text-primary dark:text-text-strong-dark leading-tight">No sales pressure</span>
                </li>
                <li class="flex items-center gap-3 justify-center text-left">
                    <span class="shrink-0 w-9 h-9 rounded-lg bg-brand-light dark:bg-brand-dark/50 flex items-center justify-center text-brand-primary dark:text-brand-primary-on-dark">
                        <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15 10.5a3 3 0 11-6 0 3 3 0 016 0z"/><path stroke-linecap="round" stroke-linejoin="round" d="M19.5 10.5c0 7.142-7.5 11.25-7.5 11.25S4.5 17.642 4.5 10.5a7.5 7.5 0 1115 0z"/></svg>
                    </span>
                    <span class="text-sm font-semibold text-text-primary dark:text-text-strong-dark leading-tight">Wirral-based, UK-wide</span>
                </li>
                <li class="flex items-center gap-3 justify-center text-left">
                    <span class="shrink-0 w-9 h-9 rounded-lg bg-brand-light dark:bg-brand-dark/50 flex items-center justify-center text-brand-primary dark:text-brand-primary-on-dark">
                        <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M2.25 8.25h19.5M2.25 9h19.5m-16.5 5.25h6m-6 2.25h3m-3.75 3h15a2.25 2.25 0 002.25-2.25V6.75A2.25 2.25 0 0019.5 4.5h-15a2.25 2.25 0 00-2.25 2.25v10.5A2.25 2.25 0 004.5 19.5z"/></svg>
                    </span>
                    <span class="text-sm font-semibold text-text-primary dark:text-text-strong-dark leading-tight">Plain-English advice</span>
                </li>
            </ul>
        </div>
    </div>
</section>

<!-- Contact Form & Info Section -->
<section class="section-padding bg-bg-section dark:bg-bg-section-dark relative">
    <div class="container relative z-10">
        <div class="max-w-4xl mx-auto space-y-12">
            
            <!-- Contact Form -->
            <div class="reveal bg-bg-surface dark:bg-bg-surface-dark rounded-xl p-8 shadow-lg border border-border-standard dark:border-border-standard-dark">
                <h2 class="text-3xl font-bold mb-6 text-text-primary dark:text-text-strong-dark">Get in touch</h2>
                <p class="text-text-secondary dark:text-text-secondary-dark mb-8 leading-relaxed">
                    If you're enquiring about a new project, please include a few details about your business and what you're hoping to achieve. The more context you share, the better advice we can give.
                </p>

                <form id="contact-form" name="contact" method="POST" data-netlify="true" netlify-honeypot="bot-field" class="space-y-6">
                    <input type="hidden" name="form-name" value="contact">
                    <p class="hidden">
                        <label>Don't fill this out if you're human: <input name="bot-field" maxlength="50" /></label>
                    </p>
                    <div>
                        <label for="name" class="block text-sm font-semibold text-text-primary dark:text-text-strong-dark mb-2">Name *</label>
                        <input 
                            type="text" 
                            id="name" 
                            name="name" 
                            required 
                            maxlength="100"
                            class="w-full px-4 py-3 rounded-lg border border-border-standard dark:border-border-standard-dark bg-bg-surface dark:bg-bg-surface-dark text-text-primary dark:text-text-strong-dark focus:border-brand-primary focus:ring-2 focus:ring-brand-primary/20 outline-none transition-all"
                            placeholder="Your name"
                        >
                    </div>

                    <div>
                        <label for="email" class="block text-sm font-semibold text-text-primary dark:text-text-strong-dark mb-2">Email *</label>
                        <input 
                            type="email" 
                            id="email" 
                            name="email" 
                            required 
                            maxlength="254"
                            class="w-full px-4 py-3 rounded-lg border border-border-standard dark:border-border-standard-dark bg-bg-surface dark:bg-bg-surface-dark text-text-primary dark:text-text-strong-dark focus:border-brand-primary focus:ring-2 focus:ring-brand-primary/20 outline-none transition-all"
                            placeholder="your@email.com"
                        >
                    </div>

                    <div>
                        <label for="phone" class="block text-sm font-semibold text-text-primary dark:text-text-strong-dark mb-2">Phone (optional)</label>
                        <input 
                            type="tel" 
                            id="phone" 
                            name="phone" 
                            maxlength="20"
                            class="w-full px-4 py-3 rounded-lg border border-border-standard dark:border-border-standard-dark bg-bg-surface dark:bg-bg-surface-dark text-text-primary dark:text-text-strong-dark focus:border-brand-primary focus:ring-2 focus:ring-brand-primary/20 outline-none transition-all"
                            placeholder="Your phone number"
                        >
                    </div>

                    <div>
                        <label for="business" class="block text-sm font-semibold text-text-primary dark:text-text-strong-dark mb-2">Business name (optional)</label>
                        <input 
                            type="text" 
                            id="business" 
                            name="business" 
                            maxlength="100"
                            class="w-full px-4 py-3 rounded-lg border border-border-standard dark:border-border-standard-dark bg-bg-surface dark:bg-bg-surface-dark text-text-primary dark:text-text-strong-dark focus:border-brand-primary focus:ring-2 focus:ring-brand-primary/20 outline-none transition-all"
                            placeholder="Your business name"
                        >
                    </div>

                    <div>
                        <label for="subject" class="block text-sm font-semibold text-text-primary dark:text-text-strong-dark mb-2">Subject *</label>
                        <select 
                            id="subject" 
                            name="subject" 
                            required 
                            class="w-full px-4 py-3 rounded-lg border border-border-standard dark:border-border-standard-dark bg-bg-surface dark:bg-bg-surface-dark text-text-primary dark:text-text-strong-dark focus:border-brand-primary focus:ring-2 focus:ring-brand-primary/20 outline-none transition-all"
                        >
                            <option value="">Select a subject...</option>
                            <option value="new-project">New website project</option>
                            <option value="essentials">Essentials website subscription</option>
                            <option value="growth">Growth website subscription</option>
                            <option value="one-off">One-off website build</option>
                            <option value="general">General enquiry</option>
                        </select>
                    </div>

                    <div>
                        <label for="message" class="block text-sm font-semibold text-text-primary dark:text-text-strong-dark mb-2">Message *</label>
                        <textarea 
                            id="message" 
                            name="message" 
                            required 
                            maxlength="3000"
                            rows="6" 
                            class="w-full px-4 py-3 rounded-lg border border-border-standard dark:border-border-standard-dark bg-bg-surface dark:bg-bg-surface-dark text-text-primary dark:text-text-strong-dark focus:border-brand-primary focus:ring-2 focus:ring-brand-primary/20 outline-none transition-all resize-vertical"
                            placeholder="Tell us about your project or enquiry..."
                        ></textarea>
                        <p class="mt-1 text-xs text-text-light text-right" id="message-counter">0 / 3000 characters</p>
                    </div>

                    <div class="flex items-start gap-3">
                        <input 
                            type="checkbox" 
                            id="privacy" 
                            name="privacy" 
                            required 
                            class="mt-1 w-4 h-4 text-brand-primary border-border-standard dark:border-border-standard-dark rounded focus:ring-brand-primary"
                        >
                        <label for="privacy" class="text-sm text-text-secondary dark:text-text-secondary-dark">
                            I agree to the <a href="/privacy-policy/" class="text-brand-primary dark:text-brand-primary-on-dark hover:underline">Privacy Policy</a> and understand my data will be processed to respond to this enquiry.
                        </label>
                    </div>

                    <button 
                        type="submit" 
                        class="btn btn--primary btn-shine w-full text-lg py-4"
                    >
                        Send message
                    </button>

                    <div id="form-message" class="hidden p-4 rounded-lg text-center"></div>
                </form>
            </div>

            <!-- Contact Information -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div class="reveal reveal--delay-1 bg-blue-50 dark:bg-blue-900/10 rounded-lg p-6 border border-blue-200 dark:border-blue-800">
                    <h3 class="font-bold mb-2 text-text-primary dark:text-text-strong-dark">Prefer a call?</h3>
                    <p class="text-sm text-text-secondary dark:text-text-secondary-dark leading-relaxed">
                        If you'd rather talk things through, we can arrange a short discovery call to discuss your goals and next steps. Just drop us an email and we'll find a time that suits you.
                    </p>
                </div>

                <div class="reveal reveal--delay-2 bg-bg-section dark:bg-bg-section-dark rounded-lg p-6 border border-border-standard dark:border-border-standard-dark">
                    <h3 class="font-bold mb-2 text-text-primary dark:text-text-strong-dark">Already a client?</h3>
                    <p class="text-sm text-text-secondary dark:text-text-secondary-dark leading-relaxed">
                        For existing clients, please use your usual support email or submit a request through your client portal so we can prioritise it correctly.
                    </p>
                </div>
            </div>

        </div>
    </div>
</section>

<script>
// Contact form handling for Netlify Forms
document.addEventListener('DOMContentLoaded', function() {
    const form = document.getElementById('contact-form');
    const formMessage = document.getElementById('form-message');
    const messageField = document.getElementById('message');
    const messageCounter = document.getElementById('message-counter');

    // Character counter for message field
    if (messageField && messageCounter) {
        function updateCounter() {
            const length = messageField.value.length;
            const maxLength = messageField.maxLength;
            messageCounter.textContent = `${length} / ${maxLength} characters`;
            
            // Change colour when approaching limit
            if (length > maxLength * 0.9) {
                messageCounter.classList.add('text-red-600', 'dark:text-red-400');
                messageCounter.classList.remove('text-text-light');
            } else {
                messageCounter.classList.remove('text-red-600', 'dark:text-red-400');
                messageCounter.classList.add('text-text-light');
            }
        }
        
        messageField.addEventListener('input', updateCounter);
        updateCounter(); // Initial count
    }

    // Client-side validation for all fields
    function validateField(field) {
        const maxLength = parseInt(field.getAttribute('maxlength'));
        if (maxLength && field.value.length > maxLength) {
            field.setCustomValidity(`This field cannot exceed ${maxLength} characters.`);
            return false;
        }
        field.setCustomValidity('');
        return true;
    }

    // Validate all fields on input
    if (form) {
        const textFields = form.querySelectorAll('input[type="text"], input[type="email"], input[type="tel"], textarea');
        textFields.forEach(field => {
            field.addEventListener('input', function() {
                validateField(this);
            });
        });

        form.addEventListener('submit', async function(e) {
            e.preventDefault();

            // Validate all fields before submission
            let isValid = true;
            textFields.forEach(field => {
                if (!validateField(field)) {
                    isValid = false;
                }
            });

            if (!isValid) {
                formMessage.className = 'p-4 rounded-lg text-center bg-yellow-50 dark:bg-yellow-900/20 text-yellow-700 dark:text-yellow-400 border border-yellow-200 dark:border-yellow-800';
                formMessage.innerHTML = 'Please check your input. Some fields exceed the maximum length.';
                formMessage.classList.remove('hidden');
                return;
            }

            // Get form data
            const formData = new FormData(form);

            // Show loading state
            const submitButton = form.querySelector('button[type="submit"]');
            const originalButtonText = submitButton.innerHTML;
            submitButton.disabled = true;
            submitButton.innerHTML = 'Sending...';
            formMessage.classList.add('hidden');

            try {
                // Submit to Netlify Forms
                const response = await fetch('/', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
                    body: new URLSearchParams(formData).toString()
                });

                if (response.ok) {
                    // Success - redirect to thank you page
                    window.location.href = '/thank-you/';
                } else {
                    throw new Error('Form submission failed');
                }
            } catch (error) {
                // Error - show fallback message
                formMessage.className = 'p-4 rounded-lg text-center bg-red-50 dark:bg-red-900/20 text-red-700 dark:text-red-400 border border-red-200 dark:border-red-800';
                formMessage.innerHTML = 'Sorry, there was an error sending your message. Please email us directly at <a href="mailto:hello@codecraftnorth.co.uk" class="underline font-semibold">hello@codecraftnorth.co.uk</a>';
                formMessage.classList.remove('hidden');
            } finally {
                // Reset button
                submitButton.disabled = false;
                submitButton.innerHTML = originalButtonText;
            }
        });
    }
});
</script>

<!-- Breadcrumb Structured Data (Schema.org) -->
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [
    {
      "@type": "ListItem",
      "position": 1,
      "name": "Home",
      "item": "https://codecraftnorth.co.uk/"
    },
    {
      "@type": "ListItem",
      "position": 2,
      "name": "Contact",
      "item": "https://codecraftnorth.co.uk/contact/"
    }
  ]
}
</script>
